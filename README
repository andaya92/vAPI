#### Huge problem

List responses need to be wrapped in a Dict with key data
because volley expects a response of JSONObjects not JSONArrays
so if all lists are wrapped in data we can check type within Java code to make a JSON array or JSONObject to return to the rest of the android app


#############



README
# https://cst-499-volunteer-api.herokuapp.com/

# Delete Migrations
find . -path "*/migrations/*.py" -not -name "__init__.py" -delete
find . -path "*/migrations/*.pyc"  -delete

# Heroku Deploy
git add .
git commit -am "make it better"
git push origin
git push heroku master


heroku login
heroku run bash


# Android
# app APK install fail
- increase AVD device memory in manager settings (click on the pencil)